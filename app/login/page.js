'use client'
import {useState} from 'react'; import {createClient} from '../../lib/supabase-browser';
export default function Login(){const s=createClient();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const submit=async e=>{e.preventDefault();setMsg('');const {error}=await s.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else location.href='/'};return <Shell title="Zaloguj się"><form onSubmit={submit} className="panel"><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Hasło<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary btnfull">Zaloguj</button>{msg&&<p className="error">{msg}</p>}<p>Nie masz konta? <a href="/rejestracja">Zarejestruj się</a></p></form></Shell>}
function Shell({title,children}){return <main className="auth"><a href="/"><img src="/images/logo.png" className="authlogo"/></a><h1>{title}</h1>{children}</main>}
