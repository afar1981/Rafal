'use client'
import {useState} from 'react'; import {createClient} from '../../lib/supabase-browser';
export default function Register(){const s=createClient();const [f,setF]=useState({email:'',password:'',name:'',company:'',phone:'',address:''});const [msg,setMsg]=useState('');const submit=async e=>{e.preventDefault();setMsg('');const {data,error}=await s.auth.signUp({email:f.email,password:f.password,options:{data:{full_name:f.name,company:f.company,phone:f.phone,address:f.address}}});if(error)setMsg(error.message);else setMsg(data.session?'Konto utworzone. Możesz się zalogować.':'Konto utworzone. Jeśli poprosimy o potwierdzenie e-maila, sprawdź skrzynkę. Konto może wymagać akceptacji przez administratora.');};return <main className="auth"><a href="/"><img src="/images/logo.png" className="authlogo"/></a><h1>Rejestracja klienta</h1><form onSubmit={submit} className="panel"><label>Imię i nazwisko<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Firma<input value={f.company} onChange={e=>setF({...f,company:e.target.value})}/></label><label>Telefon<input value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></label><label>Adres dostawy<textarea value={f.address} onChange={e=>setF({...f,address:e.target.value})}/></label><label>E-mail<input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Hasło<input type="password" minLength="8" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label><button className="primary btnfull">Utwórz konto</button>{msg&&<p className="notice">{msg}</p>}<p>Masz już konto? <a href="/login">Zaloguj się</a></p></form></main>}
